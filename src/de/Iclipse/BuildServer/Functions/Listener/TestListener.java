package de.Iclipse.BuildServer.Functions.Listener;

import org.bukkit.event.Listener;

public class TestListener implements Listener {
    /*
    @EventHandler
    public void onSneak(PlayerToggleSneakEvent e){
        if(e.getPlayer().isSneaking()) {
            e.getPlayer().sendMessage("Sneak: OFF");
        }else{
            e.getPlayer().sendMessage("Sneak: ON");
        }
    }

    @EventHandler
    public void onSneak(EntityToggleSwimEvent e){
        if(e.getEntity() instanceof Player) {
            if (((Player) e.getEntity()).isSwimming()) {
                e.getEntity().sendMessage("Swim: OFF");
            } else {
                e.getEntity().sendMessage("Swim: ON");
            }
        }
    }

    @EventHandler
    public void onSneak(EntityToggleGlideEvent e){
        if(e.getEntity() instanceof Player) {
            if (((Player) e.getEntity()).isGliding()) {
                e.getEntity().sendMessage("Glide: OFF");
            } else {
                e.getEntity().sendMessage("Glide: ON");
            }
        }
    }

    @EventHandler
    public void onPlayer(PlayerAnimationEvent e){
        e.getPlayer().sendMessage(e.getAnimationType().toString());
    }

    @EventHandler
    public void on(PlayerInteractEvent e){
        e.getPlayer().sendMessage(e.getAction().toString());
    }

    @EventHandler
    public void onP(PlayerVelocityEvent e){
        e.getPlayer().sendMessage("Velocity");
    }

    @EventHandler
    public void onSneak(EntityPoseChangeEvent e){
        if(e.getEntity() instanceof Player) {
            e.getEntity().sendMessage(e.getPose().name());
        }
    }
     */


}
